#include "main.h"
/**
 * mul - multiplies two values a and b and returns the result
 * @a : First multiplier
 * @b : second multiplier
 * Return : @res (success)
 */

int mul(int a, int b)
{
	int res;

	res = a * b;

	return (res);
}
