#include <stido.h>
/**
 * main - Entry point
 * Description: 'Prints all singel digit base 10 numbers'
 * Return: Alwyas 0 (success)
 *
 */

int main(void)
{
	int a = 0;

	if (a <= 9)
	{
		printf(a);
		a++;
	}
	printf('\n');

	return (0);
}
