#include "main.h"
/**
 * _abs - returns absolute value
 * @a : Input value of type int
 * Return: a if positive -a otherwise
 */

int _abs(int a)
{
	if (a >= 0)
	{
		return (a);
	}
	else
	{
		return (-a);
	}
}
