#include "main.h"
/**
 * _islower - determines if c is lowercase or uppercase
 * @c : the character to check
 * Return: 1 if lowercase 0 otherwise
 */

int _islower(int c)
{
	if (c >= 'a')
	{
		return (1);
	}
	else
	{
		return (0);
	}

}
